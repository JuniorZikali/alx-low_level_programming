#include <stdio.h>

/**
 * main prints the phrase "Programming is like bulding a multilingual puzzle
 * Return: 0 is exited properly, 0 otherwise
 */

int main(void)
{ 
	puts("\"Programming is like building a multilingual puzzle");
		return (0);
}
